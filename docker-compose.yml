services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: solvoku
      POSTGRES_USER: solvoku
      POSTGRES_PASSWORD: solvoku
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
